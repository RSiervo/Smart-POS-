import React from 'react';

// This is just a placeholder if we wanted a separate floating assistant.
// For this layout, the assistant is embedded in the Analytics view for better UX.
export const AIAssistant = () => {
  return null;
};